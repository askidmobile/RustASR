# Фаза 3: AuT Audio Encoder

Реализация "ушей" модели - критичный компонент для понимания аудио.

## Задачи

### 3.1. Структура Модели
- [ ] Определить `AuTEncoderConfig` с параметрами:
    * `d_model`: Размерность модели (1280 по умолчанию, 896 для 0.6B, 1024 для 1.7B)
    * `encoder_layers`: Количество слоев трансформера (32)
    * `encoder_attention_heads`: Количество attention heads (20)
    * `encoder_ffn_dim`: Размерность FFN (5120)
    * `num_mel_bins`: Входная размерность (128)
    * `downsample_rate`: Коэффициент downsampling (8x)
    * `output_token_rate`: Результирующая частота токенов (12.5 Hz)
- [ ] Создать каркас `struct AuTEncoder` с полями для всех компонентов.
- [ ] Реализовать метод `forward()` для прохода данных через энкодер.

### 3.2. Conv2D Downsampling Block
- [ ] Реализовать начальный блок сверточных слоев (Conv2D):
    * Input: `[batch, time, 128]` Mel spectrogram
    * Downsampling: 8x через несколько Conv2D слоев
    * Output: `[batch, time/8, d_model]` эмбеддинги (12.5 Hz token rate)
- [ ] Добавить Batch Normalization и активации (ReLU/GELU).
- [ ] Реализовать позиционные эмбеддинги после downsampling.

### 3.3. Transformer Encoder Layers
- [ ] **EncoderLayer** - основной блок трансформера:
    * Pre-LayerNorm архитектура
    * Multi-Head Self-Attention (20 heads)
    * Feed-Forward Network (FFN с размерностью 5120)
    * Residual connections
- [ ] **Multi-Head Attention**:
    * Стандартный self-attention (начать с этого)
    * Flash Attention (опционально, для оптимизации)
    * Dynamic attention windows (1-8 секунд контекста)
- [ ] **Position-wise FFN**:
    * Linear → Activation (GELU) → Linear
    * Dropout для регуляризации

### 3.4. Загрузка Весов
- [ ] Изучить структуру весов в `.safetensors` файле модели.
- [ ] Создать мэппинг имен весов:
    * PyTorch: `audio_model.encoder.layers.0.self_attn.q_proj.weight`
    * Rust: соответствующее поле в `EncoderLayer`
- [ ] Реализовать `load_from_safetensors()` метод.
- [ ] Добавить валидацию загруженных весов (размерности, dtypes).

### 3.5. Оптимизации
- [ ] Реализовать Flash Attention (если доступно в candle).
- [ ] Оптимизировать под Metal/CUDA (через candle backend).
- [ ] Добавить поддержку mixed precision (FP16/BF16).

## ✅ Критерии Завершения (Definition of Done)

- Энкодер успешно загружает веса из официальных `.safetensors` файлов
- Forward pass производит выход идентичный Python-реализации (допуск 1e-4)
- Код работает как на CPU, так и на GPU (Metal)
- Написаны unit-тесты для каждого компонента (Conv, Attention, FFN)
- Документация содержит описание архитектуры и API

## 📊 Метрики Успеха

- Скорость инференса: < 0.2 RTF для аудио длительностью 10 секунд на M1/M2
- Потребление памяти: < 2GB для модели 1.7B на FP32
- Точность совпадения с PyTorch: Cosine Similarity > 0.999

## ⚠️ Риски и Митигация

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Несовпадение весов между PyTorch и Rust | Высокая | Критическое | Пошаговая верификация каждого слоя |
| Проблемы с реализацией attention | Средняя | Высокое | Использовать проверенные компоненты из candle-transformers |
| Низкая производительность на CPU | Средняя | Среднее | Приоритет на GPU (Metal), оптимизация batch размера |
| Отсутствие Flash Attention в candle | Низкая | Среднее | Начать с стандартного attention, добавить Flash позже |

## 🔗 Зависимости

### Зависит от:
- **Фаза 1**: Настроенное окружение, candle
- **Фаза 2**: Feature Extractor для подготовки входных данных

### Блокирует:
- **Фаза 4**: Интеграция невозможна без рабочего энкодера

## 🧪 План Тестирования

1. **Unit-тесты компонентов**:
   - Conv2D downsampling: проверка размерностей выхода
   - Multi-Head Attention: тест с известными Q, K, V
   - FFN: проверка forward и backward pass
   - Layer Normalization: сравнение с PyTorch

2. **Integration-тесты**:
   - Полный forward pass с случайным входом
   - Сравнение выхода с PyTorch (layer-by-layer)
   - Тест загрузки весов для обеих моделей (0.6B и 1.7B)

3. **Benchmark-тесты**:
   - Измерение скорости на разных длинах входа (100, 500, 1000 frames)
   - Профилирование узких мест (attention, FFN, matmul)
   - Сравнение CPU vs Metal performance

## 🏗️ Архитектура AuT Encoder

```
Input: Mel Spectrogram [batch, time, 128]
  ↓
Conv2D Downsampling (8x)
  → Output: [batch, time/8, d_model] @ 12.5 Hz
  ↓
Positional Embedding
  ↓
Transformer Encoder Layers (×32)
  → Each Layer:
    - LayerNorm
    - Multi-Head Self-Attention (20 heads)
    - Residual Connection
    - LayerNorm
    - Feed-Forward Network (dim=5120)
    - Residual Connection
  ↓
Final LayerNorm
  ↓
Output: Audio Embeddings [batch, time/8, d_model]
```

## 📐 Размерности для разных моделей

| Модель | Encoder Params | d_model | Heads | FFN Dim | Layers |
|--------|----------------|---------|-------|---------|--------|
| 0.6B   | 180M           | 896     | 20    | 5120    | 32     |
| 1.7B   | 300M           | 1024    | 20    | 5120    | 32     |

## 📚 Референсы

- [Qwen3-ASR Technical Report (arXiv:2601.21337)](https://arxiv.org/html/2601.21337v1)
- [Qwen3-ASR-1.7B Model Card](https://huggingface.co/Qwen/Qwen3-ASR-1.7B)
- [Attention Is All You Need (Transformer Architecture)](https://arxiv.org/abs/1706.03762)
- [Flash Attention Paper](https://arxiv.org/abs/2205.14135)
